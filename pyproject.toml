[project]
name = "energy-forecast"
version = "0.1.0"
description = "Energy forecasting project using open electricity data."
requires-python = ">=3.14"

dependencies = [
    "pandas",
    "numpy",
    "matplotlib",
    "seaborn",
    "scikit-learn",
    "prophet",
    "fastapi",
    "uvicorn",
    "requests",
    "jupyter",
    "statsmodels>=0.14.5",
    "scipy>=1.16.2",
]
